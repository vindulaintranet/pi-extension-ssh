# Changelog

## 0.1.0

- Initial public-ready SSH package scaffold
- Adds remote session mode via `--ssh`
- Adds `/ssh-run` for ad-hoc commands
- Adds local SSH audit log in `.pi/ssh/ssh.log`
- Adds tests for target parsing, command building, output truncation, and logging helpers
- Adds CI validation and release docs
